<!-- <h2>Form Input User Baru</h2>
<hr>

<% if (info_error) { %>
    <strong style="color: red;">
        <% info_error.errno %> - <% info_error.code %> <br>
        <% info_error.sqlMessage %> <br>
    </strong>
    <br>
<% } %>

<div class="row">
    <div class="col-md-12 col-lg-6">
        <form action="/user/update-user/<%= users[0].id %>" method="post">

            <div class="mb-3">
                <label class="form-label">Username</label>
                <input type="text" class="form-control" name="form_username" id="form_username" value="<%= users[0].username %>">
            </div>

            <div class="mb-3">
                <label class="form-label">Password</label>
                <input type="password" class="form-control" name="form_password" id="form_password" value="<%= users[0].password %>">
            </div>
            
            <div class="mb-3">
                <label class="form-label">Nama Lengkap</label>
                <input type="text" class="form-control" name="nama_lengkap" id="nama_lengkap" value="<%= users[0].nama_lengkap %>">
            </div>

            <button type="submit" class="btn btn-secondary">
                <i class="fa-solid fa-save me-2"></i>
                Perbarui
            </button>
        </form>
    </div>
</div> -->